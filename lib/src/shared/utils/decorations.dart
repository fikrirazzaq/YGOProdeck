import 'package:flutter/material.dart';

final BoxDecoration primaryDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.circular(100),
  boxShadow: [
    BoxShadow(
      color: Colors.grey,
      offset: Offset(0, 1),
      blurRadius: 5,
    )
  ],
);
